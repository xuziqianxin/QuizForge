# QuizForge 体验完善 TODO

> 按优先级分组,每项完成一个勾选一个(`- [x]`)。每项都包含**验收标准**,
> 实现后必须补测试并保证全部测试套件全绿(当前 6 套共 472 项)。
> 开发约定:改代码前先 `python tools/backup_code.py`,改后同步 README + CHANGELOG。

## P0 第一梯队(高价值低成本,建议先做)

- [x] **01. 错题本闭环**
  - 回看界面加「加入错题本」一键收集答错/存疑题;主菜单新增"错题重练"入口,从错题本再抽题。
  - 做法:错题本用特殊分类(如 `wrongbook`)或独立 JSON,复用现有存储/抽题/去重。
  - 验收:答错题加入错题本 → 重练只含错题;重复加入不重复;测试覆盖。
  - 完成(v1.7.0~1.7.3):错题索引存于各分类文件内部 `wrong_ids` 字段;
    **每个真实分类后紧跟「XX错题」子分类**(如「虚拟化错题」),只聚合
    本学科错题,融入分类抽题流程(可配题型数量/模板/只抽部分),答对
    自动移出/答错保留;题库管理「错题」手动移除;回看「加入错题本」
    按分类归位;`wrongbook_timer_test.py` 84 项覆盖。

- [x] **02. 答题计时**
  - 答题开始计时,界面显示已用时间,可暂停/继续;提交后回看页显示"用时 12:34"。
  - 做法:`time.monotonic` + 答题页底部 Static;暂停时停表。
  - 验收:计时随题目推进、暂停停表、提交后用时写入回看页;测试覆盖(不依赖真实 sleep)。
  - 完成(v1.7.0/1.7.1/1.8.0):`ExamSession` 计时字段与方法 + 答题页**每秒实时刷新**
    计时显示 + 暂停/继续(暂停期间作答冻结)+ 回看摘要用时;**限时模拟考试**
    (倒计时显示 + 到点自动交卷,分钟数即改即存);测试不依赖真实 sleep。

- [ ] **03. 即时判题(练习模式)**
  - 答题设置增加「练习模式」开关:选完立即判对错并显示解析;考试模式保持现状(提交后统一判)。
  - 做法:ExamSession 加 mode 字段,练习模式选中后立即记录 is_correct 并展示。
  - 验收:练习模式选完即显示对错/解析且不可再改;考试模式行为不变;测试覆盖。

- [ ] **04. 全键盘操作**
  - 选择题按 `A/B/C/D` 直接选择、`Enter` 下一题、`?` 呼出快捷键帮助、`/` 快速搜索。
  - 做法:各屏幕绑定 Key 事件(注意与输入框焦点冲突)。
  - 验收:键盘选题等效鼠标;帮助页列出全部快捷键;输入框内按键不误触;测试覆盖。

- [x] **05. 备份恢复界面**
  - 设置界面加「从备份恢复」:列出 `data/backup/` 各文件备份,选一份直接还原(带二次确认)。
  - 做法:复用 storage.backup_file 的 glob 逻辑,恢复前再备份当前文件。
  - 验收:误删分类后从备份恢复出原文件;恢复前有确认;测试覆盖。
  - 完成(v1.8.2):设置 -> 备份恢复弹窗列出全部备份(文件名/时间/大小),
    选中 + 二次确认后还原;恢复前自动备份当前文件;恢复 `index.json`
    自动刷新统计;`list_backups`/`restore_backup` 服务层 + UI 测试覆盖。

- [ ] **06. 深浅主题切换**
  - 设置里加主题选项(深色/浅色),theme.py 改为变量化两套配色。
  - 验收:切换后立即生效且布局无回归;设置项持久化;测试覆盖。

## P1 第二梯队(中成本中价值)

- [x] **07. 历史成绩记录**
  - 每次提交成绩(自动分/总分/正确率/用时/题型分布)追加到 `data/history.json`;主菜单或设置可查看最近 N 次。
  - 验收:多次提交后记录完整、按时间倒序展示;单条记录可删除;测试覆盖。
  - 完成(v1.8.3/1.8.4):每次提交自动记录(含时间/模式/来源/得分/正确率/
    用时/题型分布/**逐题作答详情**,最多 200 条);设置 -> 历史成绩界面
    按时间倒序展示,可**查看逐题作答详情**、**导出 HTML 试卷**、删除
    单条/清空全部(带确认);`HistoryStore` 服务层 + UI 测试覆盖。

- [x] **08. 导出 HTML 试卷**
  - 题库管理导出增加「HTML 试卷」选项:题干+选项+答题区,浏览器打印成 PDF 纸质练。
  - 做法:rich 内置 HTML 导出或手写模板;与 JSON 导出并列。
  - 验收:导出 HTML 打开后题目完整、选项带字母、可勾选/填写;测试覆盖。
  - 完成(v1.8.4):题库管理「导出HTML」把当前检索结果导出为 HTML 试卷
    (题干 + 选项带字母 + 作答区:选择题可勾选、填空/简答可填写,打印
    成纸质练习);**历史作答记录同样可导出为 HTML**(含判分标注);
    `html_exporter.py` 服务层 + UI 测试覆盖,全部文本 HTML 转义防注入。

- [x] **09. 导入报告细化**
  - 导入后显示"新增 X / 跳过 Y"并列出跳过的题,方便发现"作业更新了但题库没变"。
  - 验收:重复导入时跳过列表可见;测试覆盖。
  - 完成(v1.8.5):题库管理导入与学习通导入完成后弹窗显示新增/跳过数,
    并列出跳过的题目题干(最多 50 条,超出标注剩余);`ImportSummary`
    新增 `skipped_questions` 与 `skipped_detail_text`;测试覆盖(服务层
    收集/截断 + UI 端到端重复导入全部跳过)。

- [ ] **10. 答题进度常驻**
  - 答题页顶部常显"第 3/20 题 · 已答 15 · 单选",小窗口下比答题卡更直观。
  - 验收:随答题推进实时更新;80×24 下不换行不裁切;测试覆盖。

- [ ] **11. 主菜单数据概况增强**
  - 现有只显示分类数/总题数;加每题型的题量分布与最近答题正确率。
  - 验收:统计准确、无题库时不报错;测试覆盖。

## P2 第三梯队(工程向/可选)

- [ ] **12. 跨分类搜索**
  - 检索加「全库搜索」开关:跨分类匹配,命中处标注所属分类。
  - 做法:复用 bank_service 的 find_questions_by_text。
  - 验收:跨分类结果带分类名;测试覆盖。

- [x] **13. 分页浏览替代单次 500 截断**
  - 表格/检索改为"上一页/下一页"分页,大题库可看全量。
  - 验收:翻页正确、边界(最后一页)无空页;测试覆盖。
  - 完成(v1.8.1):题库管理/查题每页 50 题分页浏览(「第 X / Y 页」+
    上一页/下一页,末页无空页);`bank_service.search` 支持 offset,
    新增 `count_matches` 统计总数;测试覆盖(服务层分页 + UI 端到端
    翻页/末页/按钮状态)。

- [ ] **14. 学习通作业截止提醒**
  - 拉取作业列表时顺带解析截止时间,主菜单显示"最近到期作业"。
  - 验收:有截止字段的作业正确解析并展示;无字段时优雅降级;测试覆盖。

- [x] **15. 打包 exe**
  - PyInstaller 一键打包,学生用户无需装 Python(注意 Textual 打包体积与 UPX 兼容)。
  - 验收:打包产物在干净 Windows 上可启动、答题/导入功能正常。
  - 完成(v1.8.8):`tools/build_exe.py` 一键打包单文件 `dist/QuizForge.exe`
    (约 39 MB,`--collect-all textual/rich`);`main.py --selftest` 无头
    自检(题库读写/评分/UI 渲染),源码与 exe 均验证通过。

- [ ] **16. 数据目录可配置**
  - 支持 `--data-dir` 参数或环境变量,方便题库放 U 盘/网盘同步。
  - 验收:指定目录后数据读写全部落在该目录;测试覆盖。

- [ ] **17. 测试质量再升级**
  - 加覆盖率统计(coverage.py);更多窗口尺寸(如 100×30、40×20)UI 布局测试;解析器加更多真实页面样本。
  - 验收:覆盖率报告可生成;新尺寸布局无裁切;样本解析全部通过。

## 明确不做(供讨论,不实现)

- **本地作答回填学习通**:把本地答题结果回填平台属于"代刷作业"风险行为,不做。
- 其他:账号密码明文保存(绝不)、绕过学习通反爬的高频抓取(违反使用条款)。
