# QuizForge 更新日志

本项目采用"修改前备份、改后更新 README 与 CHANGELOG"的开发约定:
每次代码修改前运行 `python tools/backup_code.py` 保留一份源码备份。

## [1.8.8] - 2026-08-16 打包 Windows exe(TODO 15)

### 新增
- **PyInstaller 一键打包**:新增 `tools/build_exe.py`,产出单文件
  `dist/QuizForge.exe`(约 39 MB,含 Python 与全部依赖,目标机器无需
  安装 Python)。`--collect-all textual/rich` 保证组件完整。
- **无头自检**:`main.py --selftest` 验证题库读写、评分、Textual UI
  无头渲染;打包后的 exe 同样支持,便于诊断分发版本是否完整。

### 验证
- 源码与 exe 的 `--selftest` 均通过(题库 1 题、评分 1/1、UI 渲染正常),
  确认打包产物模块完整、可启动。

## [1.8.7] - 2026-08-16 撤销 CJK 补丁,彻底修复渲染重影

### 修复
- **渲染重影根治**:彻底撤销自定义 CJK 补丁(`cjk_patch.py` 变为 no-op)。
  此前为修复下拉框中文缺字,monkey-patch 了 rich `Segment._split_cells`
  把双宽字符完整保留,但破坏了 rich 的宽度记账契约(段实际宽度比
  cut 多 1 列),导致 Compositor 拼接浮层(Toast 提示框)时各段错位
  重叠——**渲染重影**。
- **接受缺字**:下拉框中文在极少数窗口宽度下少一个字符,是 rich 官方
  设计行为(`split_cells` 文档明示"双宽字符被切时变成两个空格"),
  影响极小,予以接受;宽度计算本身由 rich 内置的 wcwidth 表
  (`_cell_widths.CELL_WIDTHS`)保证正确,无需也不应重写。

### 测试
- `tests/smoke_test.py` 调整为**渲染一致性**断言(divide/crop 记账
  一致、任意切点总宽守恒、下拉浮层渲染无异常),不再断言字符完整性。

## [1.8.6] - 2026-08-16 修复提示框重影(CJK 补丁记账一致性)

### 修复
- **提示框(Toast)重影**:CJK 渲染补丁让双宽字符完整保留在左侧段后,
  该段实际宽度比切割点多 1 列,而 `Strip.divide` / `Strip.crop` 仍按
  `cut - pos` 记账,导致 Compositor 拼接浮层时各段错位重叠(重影;
  点击全量重绘后暂时恢复)。
- **修复**:`cjk_patch.py` 同时替换 `Strip.divide` 与 `Strip.crop`,
  让它们按每段**实际显示宽度**记账(而非记账宽度),字符保留与宽度
  一致兼得。

### 测试
- `tests/smoke_test.py` 201 → 205 项:新增 4 项 CJK 渲染一致性断言
  (divide 记账一致/总宽守恒/crop 记账一致/任意切点重建一致)。

## [1.8.5] - 2026-08-16 导入报告细化

### 变更
- **导入报告细化**(TODO 09):题库管理导入与学习通导入完成后弹出
  **详细报告**——新增/跳过数量 + **列出跳过的题目题干**(最多 50 条,
  超出显示"其余 N 条略"),便于发现"作业更新了但题库没变"。
- `ImportSummary` 新增 `skipped_questions`(跳过题题干)与
  `skipped_detail_text()`(格式化报告文本,含截断)。

### 测试
- `tests/smoke_test.py` 196 → 201 项:跳过详情收集(重复题题干)、
  截断到上限、标注剩余条数、无跳过时为空。
- `tests/wrongbook_timer_test.py` 154 → 162 项:UI 端到端(导入后弹窗
  显示新增/跳过数并列出跳过题;重复导入全部跳过)。
- `tests/ui_walkthrough.py` 适配:导入后需关闭报告弹窗再断言。

## [1.8.4] - 2026-08-16 历史作答详情 + HTML 试卷导出

### 新增功能
- **历史作答详情**:每次提交的记录新增 `details` 逐题详情(题干/选项/
  你的作答/正确答案/对错/解析);历史成绩界面选中记录点「查看作答」
  弹出详情弹窗(题目列表 + 逐题详情)。
- **HTML 导出**(TODO 08):
  - 历史作答记录可导出为 HTML(含判分标注:正确/错误/未答,浏览器
    可查看/打印);
  - 题库管理新增「导出HTML」:把当前分类检索结果导出为 HTML 试卷
    (题干 + 选项带字母 + 作答区:选择题可勾选、填空/简答可填写),
    打印成纸质练习;
  - 所有文本经 HTML 转义防注入。
- 新增 `quizforge/services/html_exporter.py`(`export_record_html` /
  `export_questions_html`)。

### 测试
- `tests/smoke_test.py` 184 → 196 项:HTML 导出服务层(记录含判分标注/
  无详情兜底/试卷带答题区/答案版/转义防注入)。
- `tests/wrongbook_timer_test.py` 140 → 154 项:UI 端到端(提交后记录
  含逐题详情、详情弹窗查看、详情弹窗导出 HTML、历史界面导出 HTML、
  题库管理导出 HTML 试卷)。
- 题库管理工具栏新增按钮,min-width 7 保证 80x24 下全部可见。

## [1.8.3] - 2026-08-16 历史成绩记录

### 新增功能
- **历史成绩自动记录**:每次提交答题后把成绩追加到 `data/history.json`
  (最多保留 200 条,超出丢弃最旧)。记录含:提交时间、答题模式
  (exam/wrongbook)、来源分类、自动分/总分、正确/错误/未答/未设答案
  题数、用时(秒)、题目总数、题型分布。
- **历史成绩界面**:设置 -> 历史成绩,按时间倒序展示(时间/来源/得分/
  正确率/用时/题型分布,分页浏览),支持删除单条与清空全部(均带二次
  确认)。
- `ExamConfig` 新增 `category_id`(抽题来源分类,历史记录标记来源用)。

### 测试
- `tests/smoke_test.py` 174 → 184 项:`HistoryStore` 增删查、按时间倒序、
  上限丢弃最旧、损坏兜底、文件结构。
- `tests/wrongbook_timer_test.py` 126 → 140 项:UI 端到端(提交后自动
  生成记录、历史按时间倒序、记录含得分/用时/题型分布、错题重练来源
  标记、界面展示、删除单条、清空)。

## [1.8.2] - 2026-08-16 备份恢复界面

### 新增功能
- **设置 -> 备份恢复**:列出 `data/backup/` 全部备份(原文件名、备份
  时间、大小),选中一份二次确认后还原到其目标位置;恢复前自动备份
  当前文件,误恢复可再还原。
- 恢复分类索引(`index.json`)时按恢复后的题库文件自动刷新统计信息。
- 存储层新增 `list_backups()` / `restore_backup()` / `BackupEntry`
  (备份文件名解析、目标路径推导、恢复时旧备份被清理的回退保护)。

### 测试
- `tests/smoke_test.py` 164 → 174 项:备份列表解析、目标路径推导
  (bank/根目录)、恢复后内容与备份一致、恢复前自动备份、恢复索引
  后统计刷新。
- `tests/wrongbook_timer_test.py` 119 → 126 项:UI 端到端(设置内打开
  恢复弹窗、选中备份、二次确认、恢复后回到设置界面、内容与备份一致)。

## [1.8.1] - 2026-08-15 分页浏览替代单次 500 截断

### 变更
- **题库管理 / 查题改为分页浏览**:每页最多 50 题,底部「◀ 上一页 /
  下一页 ▶」翻页并显示「第 X / Y 页」,大题库可翻页看全量(替代原
  "单次最多显示 500 条"的截断)。翻页后行号按本页重新编号;末页不会
  出现空页。
- `bank_service.search()` 新增 `offset` 参数(与 `limit` 配合分页),
  新增 `count_matches()` 统计匹配总数用于计算总页数。
- 布局:新增 pager 行样式(固定高度、页码信息 `width: auto`,避免
  窄窗口下按钮被挤出可视区)。

### 测试
- `tests/smoke_test.py` 160 → 164 项:search offset 分页(首/中/末页、
  越界返回空)、count_matches 总数统计。
- `tests/wrongbook_timer_test.py` 105 → 119 项:UI 端到端分页(题库
  管理 122 题翻 3 页、末页 22 题无空页、页码与按钮禁用状态、返回
  上一页;查题同样分页)。

## [1.8.0] - 2026-08-15 限时模拟考试(倒计时 + 到点自动交卷)

### 新增功能
- **限时模拟考试**:答题设置新增「限时模拟考试(分钟)」输入框
  (0/留空 = 不限时),即改即存到全局配置(`config.json` 新增
  `time_limit_minutes` 字段)。
- **倒计时显示**:限时模式下答题界面显示「剩余 MM:SS」(每秒刷新),
  不限时保持「用时 MM:SS」正计时。
- **到点自动交卷**:时间耗尽时自动保存当前作答并交卷评分进入回看
  (无需手动提交,未答题按未答计分);暂停期间倒计时冻结。
- 设置界面同步展示限时状态(分钟数/不限时)。

### 测试
- `tests/wrongbook_timer_test.py` 84 → 105 项:新增限时倒计时服务层
  (time_remaining/format_remaining/超时钳制/不限时 -1/满小时格式)与
  UI 端到端(配置即改即存、会话携带时长、倒计时显示、到点自动交卷
  进回看、不限时正计时)。

## [1.7.3] - 2026-08-15 错题改为每分类独立的「XX错题」子分类

### 变更(按用户反馈修正)
- **每个真实分类后紧跟其「XX错题」虚拟分类**:如「虚拟化」之后是
  「虚拟化错题」、「网络」之后是「网络错题」。错题子分类只聚合
  **本学科**的错题索引(分类文件内部 `wrong_ids` 字段),不再把所有
  学科错题堆进一个聚合分类——用户选择「虚拟化错题」只做虚拟化的错题。
- 错题子分类 id 规则:`wrongbook:<真实分类id>`(如 `wrongbook:abc123`),
  显示名 `<分类名>错题`;提供 `wrongbook_id_of` / `is_wrongbook_id` /
  `real_category_of` 工具函数。
- 答题设置/题库管理/查题的分类下拉均出现各错题子分类;选中后走与
  普通分类完全相同的分类抽题流程(题型数量/模板/超上限修正),答对
  自动移出本分类错题索引,答错保留。
- 题库管理/查题在错题子分类下禁用添加/编辑/删除/导入/导出(或定位
  到真实分类编辑);删除分类按钮禁用。
- index.json 统计与上一版保持一致(各分类 count + 顶层 total)。

### 测试
- `tests/wrongbook_timer_test.py` 扩充至 84 项:错题子分类(每分类
  独立、不混学科、紧随真实分类、id 工具函数、抽题/统计/检索、移除
  互不影响)、index 统计、计时、UI 端到端(选「数学错题」分类走分类
  抽题)与暂停冻结。
- 冒烟测试 160 项(分类断言适配子分类);全流程测试 43 项、键盘驱动
  31 项(修正分类名精确匹配,避免误选「XX错题」)。

## [1.7.2] - 2026-08-15 错题改为虚拟分类,融入分类抽题流程

### 变更(按用户反馈重构)
- **错题索引放入分类文件内部**:每个分类题库文件 `<id>.json` 增加
  `wrong_ids` 字段(只存答错题目的 id,去重、按加入顺序),加载错题按
  此索引从本文件读取,不再使用独立 wrong 文件。
- **「错题」成为虚拟分类**:`list_categories()` 末尾追加「错题」分类,
  聚合各真实分类的错题索引。答题设置/题库管理/查题的分类下拉都会出现,
  选中后走**与普通分类完全相同的分类抽题流程**——按题型配置抽取数量、
  超上限自动修正、模板保存/加载/删除全部可用,**错题太多时可只抽部分**。
- **取消答题模式的第三个「错题重练」选项**:错题作为分类融入现有
  「分类抽题」流程,不再单独占用选题方式。
- **答对自动移出**:从「错题」分类开始答题时,会话标记 wrongbook,
  提交后答对的题目自动移出所属真实分类的错题索引;答错保留。
- **回看/查题保护**:「错题」是聚合视图,回看页隐藏「加入错题本」按钮;
  题库管理/查题在错题分类下禁用添加/编辑/删除/导入/导出(或定位到
  真实分类编辑)。
- **index.json 增加统计信息**:顶层 `total`(总题目数)与各分类
  `count`(题目数),索引写入时实读刷新,题目增删时轻量更新。

### 测试
- `tests/wrongbook_timer_test.py` 重写并扩充至 75 项:错题索引存储
  (分类文件内部 wrong_ids)、错题虚拟分类(聚合/按题型统计/检索/去重)、
  index 统计、计时停表与格式化、UI 端到端(答错 -> 加入错题 -> 错题
  分类走分类抽题 -> 答对自动移出/答错保留 -> 错题多时只抽部分)与
  暂停冻结交互。
- 冒烟测试 160 项(分类列表断言适配「错题」虚拟分类)。

## [1.7.1] - 2026-08-15 错题本按分类重构 + 计时实时刷新

### 变更(按用户反馈重构)
- **错题本改为按分类记录**:废除全局 `data/wrongbook.json`,改为每个
  分类一个独立错题文件 `data/bank/<分类id>.wrong.json`(与题库文件
  并列),不同学科错题互不混淆。删除分类连带删除错题文件;删除题目
  自动从错题记录清理。
- **错题重练入口移至答题设置界面**:选题方式新增「错题重练」第三项,
  按当前所选分类直接练习该分类错题,不再占用主菜单入口(主菜单回到
  6 个按钮)。**答对的题目自动移出错题记录**,答错保留继续重练;重练
  模式回看界面不再显示「加入错题本」按钮。
- **错题可手动移除**:题库管理新增「错题」按钮,打开错题管理弹窗,
  可逐题移除或全部清空当前分类错题。
- **加入错题本按分类归位**:回看界面「加入错题本」将答错题目加入其
  所属分类的错题记录(跨分类题目各自归位)。

### 修复与改进
- **计时器实时刷新**:答题界面每秒更新"用时"显示(`set_interval`)。
- **暂停期间冻结作答**:暂停计时后上一题/下一题/答题卡/提交与全部
  作答控件禁用,只能点「继续」恢复(按钮文案同步切换 暂停/继续);
  恢复后按当前题号还原导航按钮状态。

### 测试
- `tests/wrongbook_timer_test.py` 重写并扩充至 64 项:按分类错题存储
  (去重/批量/移除/清空/顺序/损坏兜底/删除分类与题目联动)、跨分类
  题目定位、计时停表与格式化(不依赖真实 sleep)、UI 端到端(答错 ->
  按分类加入错题本 -> 题库管理手动移除 -> 错题重练答对自动移出/答错
  保留)与暂停冻结交互。
- 全流程测试入口数恢复 6 个(43 项);冒烟测试 160 项不变。

## [1.7.0] - 2026-08-15 错题本闭环 + 答题计时

### 新增功能
- **错题本**(独立 JSON `data/wrongbook.json`):回看界面「加入错题本」
  一键把本次**答错**的题目批量加入(去重,重复加入不计数);主菜单
  新增「📕 错题重练」入口,跨分类按 id 找回错题直接开始答题;已从
  题库删除的题目自动清理;主菜单统计行显示错题数量。
- **答题计时**:进入答题自动开始计时,界面底部实时显示"用时 MM:SS";
  支持「暂停/继续」(暂停期间不再累加);提交后回看摘要显示本次用时。

### 修复
- `ExamScreen._update_timer` 方法名与 Textual `Screen` 内部计时器属性
  冲突导致布局刷新崩溃,重命名为 `_refresh_timer_display`。

### 测试
- 新增 `tests/wrongbook_timer_test.py`(46 项):错题本存储(去重/批量/
  移除/清空/顺序/损坏文件兜底)、计时停表语义与格式化(**不依赖真实
  sleep**)、UI 端到端(答错 → 加入错题本 → 错题重练 → 全对不再新增 →
  空错题本提示)。
- 冒烟测试 159 → 160 项(主菜单 7 按钮布局可达性);全流程测试 43 项
  (主菜单入口数 6 → 7)。

## [1.6.4] - 2026-08-15 多选题评分边界测试

- `exam_accuracy_test.py` 新增**多选题边界测试**(9 项):全对/倒序/
  纯少选(A 缺 C、C 缺 A)/纯多选(多选 D)/错选/部分对+错选/
  **重复勾选(A,A,C 去重后判对)**/空选未作答。全部通过,证明
  `_grade_one` 的集合比较逻辑正确。
- 专项测试 14 → 23 项。

## [1.6.3] - 2026-08-15 答题正确性专项测试

- 新增 `tests/exam_accuracy_test.py`(14 项):服务层评分准确性(五种
  题型全对/全错/未答/多选少选/多选顺序无关/无答案不评分)、**选项乱序
  后答案映射不破坏**(10 组种子全对)、题目乱序生效、排除无答案开关
  的抽题过滤与上限截断。
- 新增 `tests/exam_accuracy_ui_test.py`(8 项):UI 端到端真实点击作答
  并核对回看得分——顺序作答故意答错单选显示 3/4;开启随机选项顺序
  后按正确答案作答显示 4/4,证明随机设置生效且不误判。
- 验证结论:答题评分正确(含选项乱序场景),随机题目/选项/排除无答案
  三个设置均实际生效。

## [1.6.2] - 2026-08-15 开关归位:答题设置即改即存,设置界面去重

### 变更
- **「排除无答案题目」开关移至答题设置界面**:与「随机打乱题目
  顺序」「随机打乱选择题选项顺序」三个开关并列,切换即改即存到
  全局配置;排除开关变化后已配置的抽题会立即按新规则重新抽取。
- **设置界面去重**:移除三个开关(此前与答题设置重复且需手动保存),
  改为只读展示当前开关状态 + 学习通登录管理 + 数据概况;「保存并
  返回」按钮移除,直接返回即可。两处界面看到的是同一份全局配置,
  不再出现状态不一致。

### 测试
- 冒烟测试 159 项(不变);全流程测试 42 → 43 项(答题开关即改即存);
  键盘驱动测试 32 → 31 项(设置保存流程移除)。

## [1.6.1] - 2026-08-15 修复默认分类缺失

- **修复:题库管理分类下拉看不到「默认分类」**。根因:旧数据或外部
  修改导致 `data/bank/index.json` 缺失 default 项(且 `default.json`
  文件不存在)。现 `_read_index` 读取索引时自动补齐:索引缺 default
  项则补入,题库文件缺 default.json 则直接初始化空文件(不经
  save_questions,避免经 category_file 递归)。已有数据启动时自动修复。
- 补 2 项回归测试(索引缺默认分类补齐 + 题库文件自动创建)。

## [1.6.0] - 2026-08-15 README 新增操作指南章节

- README「功能说明」重构为**「操作指南」独立章节**:按真实使用流程
  分 6 节逐步说明——启动与主菜单、题库管理(分类/添加/编辑/删除/
  检索/导入导出)、查题、答题练习(分类抽题/文件选题/答题界面/回看)、
  设置、从超星学习通导入。每节含按钮名称、字段说明、步骤与注意事项。
- 原功能说明精简为「功能特性」概览;学习通题型识别规则保留在
  操作指南第 6 节。

## [1.5.9] - 2026-08-15 键盘驱动全功能测试

- 新增 `tests/ui_walkthrough2.py`:以键盘操作为主的全功能测试(32 项),
  模拟真实终端用户操作(Tab/方向键/Enter/Escape 导航、键盘输入文本),
  覆盖键盘导航、键盘建分类/加题、重命名分类、抽题模板完整流程、
  键盘作答与答题卡跳转、学习通界面、设置开关键盘切换、无答案题
  标记与排除。
- 测试期间确认:模板加载受"超上限自动修正"约束属预期行为(空分类
  无题时上限为 0,加载含数量的模板会被修正),已在测试中预置数据。

## [1.5.8] - 2026-08-15 全流程走查测试 + 修复新建分类不选中

### 新增
- **`tests/ui_walkthrough.py`**:模拟真实用户操作的全流程测试(42 项),
  按用户自然使用顺序驱动界面,覆盖主菜单、题库管理(建分类/四种题型/
  检索/编辑/删除/导出导入)、查题、分类抽题答题(抽题配置/答题卡/提交/
  回看)、再练一次(配置保留)、文件选题(JSON 三种格式)、设置(开关生效)、
  无答案题(醒目标记/排除开关)。

### 修复
- **新建分类后不自动选中新分类**:题库管理新建分类后,
  `_reload_categories()` 的自动选择覆盖了刚设置的分类,导致用户接着
  添加的题目实际落入"默认分类"。改为保留新分类为当前选中
  (`auto_pick=False`)。冒烟测试补 1 项断言。

### 测试
- 冒烟测试 156 → 157 项;新增全流程走查测试 42 项,均稳定通过。

## [1.5.7] - 2026-08-15 无答案题醒目标记 + 排除无答案抽题开关

### 新增
- **无答案题醒目标记**:题库表格与查题结果中,未设置标准答案的题目
  答案列以 **黄色 ⚠ 未设答案** 醒目显示;查题详情页同样以醒目样式
  提示,一眼可辨。
- **「分类抽题时排除无答案题目」开关**(设置界面,持久化到
  `config.json` 的 `exclude_no_answer`,默认关闭):开启后分类抽题
  只抽有标准答案的题,抽题设置中的各题型上限同步按过滤后的数量
  显示,避免抽到无法自动评分的无答案题。

### 测试
- 冒烟测试 152 → 156 项(醒目标记、开关持久化、抽题过滤、上限
  过滤共 4 项)。

## [1.5.6] - 2026-08-15 README 数据格式文档

- README「数据格式」扩充为**全部 JSON 格式说明**(共 8 种):分类索引、
  分类题库文件、应用配置、抽题配置模板、学习通登录态、题库导入导出、
  文件选题 JSON、备份文件。每种格式附完整示例与字段说明,并逐一与
  代码实际输出核对一致。

## [1.5.5] - 2026-08-15 无答案题目全系统兜底

无答案题目(如学习通导入、答案缺失)在**存储、抽题、识别、作答、评分、
展示**各环节的系统性兜底,杜绝崩溃与空白显示:

- **存储/模型**:`Question.answer_text()` 无答案时统一返回「未设置」
  (此前返回空串,在题库表格、查题详情等处显示成空白);
- **识别**:学习通解析产出的无答案题照常入库(已支持),编辑保存时
  允许保留空答案(1.5.4 已修,本次补全其余环节验证);
- **抽题**:题目池含无答案题时正常抽取(不崩溃),评分时计入
  「未设答案」不参与自动评分;
- **作答/回看**:无答案题照常作答;回看摘要统计「未设答案 N 题」,
  详情提示「正确答案: (未设置,可在题库管理中补充)」;
- **抽题预览**:预览列表中无答案题标注 [未设答案],用户抽题前即可
  知晓哪些题无标准答案。
- 补 8 项回归测试:服务层(无答案评分/抽题) + UI 全链路(抽题预览
  标注→作答→提交→回看统计与提示)。

## [1.5.4] - 2026-08-15 编辑崩溃修复 + 默认分类改名

### 修复
- **编辑学习通导入的题目崩溃(用户反馈)**:两个根因——
  - 编辑**非默认分类**的题目时,`get_question` 未传分类 id,跨分类查
    不到题目导致编辑界面刚打开就关闭(表现为"点编辑没反应/崩溃");
  - 学习通导入的题目常**无正确答案**,编辑保存时答案校验强制要求
    答案,导致无法保存。现编辑时允许保留空答案(与导入一致),
    错误答案仍会被拦截。
- 补回归测试:服务层无答案题编辑 + UI 非默认分类编辑(新建分类→
  导入→编辑→保存→验证落盘)。

### 变更
- 默认分类名称由「未分类」改为**「默认分类」**(更直观,用户能看出
  是系统默认);旧数据索引中名为"未分类"的默认分类在读取时自动
  迁移改名(id 不变,不影响数据文件)。

## [1.5.3] - 2026-08-15 渲染修复 + 默认分类保护 + 多选识别增强

### 修复
- **下拉框中文选项缺字(用户反馈)**:题库管理/查题界面题型下拉在部分
  窗口宽度下选项文字缺字(如"单选题"显示为"单选/选题/单 题",缺字位置
  随宽度变化)。根因是 Textual 合成器按控件边界切割浮层时,切割点落在
  CJK 双宽字符中间,rich 把字符替换为空格。新增
  `quizforge/ui/cjk_patch.py` 渲染补丁:切割双宽字符时完整保留字符,
  经 40~120 列全尺寸验证无缺字;补回归测试(5 种宽度)。
- **默认分类「未分类」可被删除**:存储层拒绝删除默认分类(返回 False),
  题库管理界面选中默认分类时禁用「删除分类」按钮,并给出提示。
- **学习通多选题识别失败(用户反馈)**:识别算法增强——
  - 题干题型提示词优先(多选/(多选)/[多选] 等),答案区缺失时不再误判
    为填空/简答;
  - 正确答案无分隔符(如 `AC`)逐字母拆分,不再被当成单个键而误判单选;
  - 答案带解析等杂质文本时回退提取字母;
  - checkbox 表单页直接判定多选;input value 为数字时回退 A/B/C 键;
  - input 选项文本取 label 内文本,修复 label 包裹 input 结构取空问题。
  - 补 6 组多选识别样本测试。

### 测试
- 冒烟测试 125 → 137 项(多选识别 6 项 + CJK 渲染回归 5 项 + 默认分类保护 1 项)。

### 文档
- 新增 `docs/TEXTUAL_CJK_BUG.md`:Textual 浮层 CJK 缺字 bug 的完整调查
  过程、根因分析、运行时补丁与独立复现脚本,可直接用于向 Textual
  官方仓库提交 issue / PR。

## [1.5.2] - 2026-08-15 体验完善清单

- 新增 `TODO.md`:分级(高/中/低优先)待办清单,每项含验收标准与测试要求,
  作为后续迭代的逐项实现依据。
- `tools/backup_code.py` 备份目标纳入 `TODO.md`,清单变更同样留档。

## [1.5.1] - 2026-08-15 备份可靠性 + 测试稳定性

- 数据备份文件名升级为毫秒级时间戳:同一秒内多次写入不再互相覆盖,
  每次写入前都保留独立备份(仍保留最近 5 份)。
- 修复冒烟测试「删除分类前自动备份」偶发失败(10 次运行约 3 次):
  原断言恰好 1 份备份,而删除前分类文件被多次写入、跨秒时会保留多份;
  改为断言"删除前确有备份",消除对运行时机的依赖。

## [1.5.0] - 2026-08-15 抽题配置自动保留

- 抽题配置(各题型抽取数量)按分类记忆:答题系统内「再答一次」自动复用
  上次配置并重新抽题预览,无需重复手动设置;切到其他分类再切回同样
  恢复该分类配置,直到用户主动修改。

## [1.4.0] - 2026-08-15 代码质量审查修复 + 备份机制

### 新增
- 开发侧代码备份工具 `tools/backup_code.py`:修改代码前自动备份源码到 `_dev_backup/`,保留最近 10 份。
- 应用层数据自动备份:所有数据文件(分类题库/索引/模板/配置)写入前备份到 `data/backup/`,保留最近 5 份,防止不可逆操作损坏数据。
- **删除分类(连同其中题目)前自动备份**,误删可从 `data/backup/` 恢复;README 补充数据恢复方法。

### 修复(代码质量审查)
- **数据正确性**:导入时选项键重排不同步答案映射导致静默丢题;损坏的题库/索引文件导致崩溃;旧版题库迁移 `os.rename` 在备份存在时导致启动崩溃;损坏索引被静默覆盖丢失分类映射。
- **安全(学习通)**:登录落地地址明文 HTTP 改 HTTPS;会话失效静默误报"课程无作业"改为明确提示;多页作业重复入库(按 work_id 去重);总页数无上限防御(封顶 100 页)。
- **UI**:学习通屏幕实例复用残留旧题目可导入错误分类;resume 重置用户分类选择(显示 A 分类、题目来自 B);删除 `_on_draw_config` 重复死代码;密码登录/登出未取消二维码轮询;空白分类 `str(None)` 静默无结果;回看/抽题设置/导入/保存的异常兜底。
- **测试**:修复 15 处 `query(...) is not None` 恒真断言(此前检查实际未生效);答题卡跳转真空验证;测试数据目录隔离。
- **工程**:`.gitignore` 补 `data/bank/`/`data/templates.json`(真实题库会被误提交);新增 `pyproject.toml`;`textual` 版本上限 `<9`;启动脚本补 pycryptodome 探测;README 数据格式段更新为分类存储。

## [1.3.0] - 2026-08-15 答题/设置/布局体验

- 答题卡由 Button 网格改为 DataTable(渲染错乱修复),高亮行跳转。
- 二次作答计分板残留第一次结果(屏幕实例复用)修复:回看屏 resume 全量刷新。
- 抽题设置确认后回到设置界面并预览**具体抽中的题目**,不再直接进入答题。
- 答题设置 `ContentSwitcher` 面板高度撑满屏幕导致"开始答题"按钮不可见修复。
- 设置界面同步:学习通登录状态查看/清除、数据概况(分类/模板/路径)与当前存储结构一致。
- 各屏幕进入时自动选中第一个非空分类;小窗口布局自适应(滚动可达)。

## [1.2.0] - 2026-08-15 分类系统与答题系统重构

- **分类化题库**:按学科/课程分类,每分类独立 JSON 文件(`data/bank/`),支持新建/重命名/删除分类;学习通导入自动创建课程同名分类。
- 内存优化:分类按需加载 + LRU 缓存(同时保留 ≤4 个分类)、表格/检索显示上限 500 条。
- **答题两种选题方式**:分类抽题(按题型设置抽取数量、超上限自动修正、配置模板保存/加载/删除);文件选题(JSON id/题干列表或每行一个题干)。
- 抽题无放回、随机源系统熵;答题设置显示题目预览。
- 题库文件删除/外部修改后自动重载(文件签名检测)。

## [1.1.0] - 2026-08-15 稳定性与学习通增强

- 修复 worker 异常导致整个应用退出(统一 `exit_on_error=False` + 分组)。
- 修复设置界面重复控件 id 崩溃;题库管理编辑/删除按钮无效(改用 RowHighlighted)。
- 学习通:作业列表多页抓取(`pageNum`)、viewWork 类作业支持、内联"正确答案"提取、题干分值清理、无作业课程返回空列表。

## [1.0.0] - 2026-08-14 初始版本

- Python + Textual 终端题库工具:题库管理(增删改查/检索/JSON 导入导出)、答题练习(随机排序开关、答题卡、跳题、回看)、查题、设置。
- 超星学习通作业拉取:二维码/账号密码登录、课程/作业列表、题目解析(2026 年接口实测)。
- 启动脚本(start.bat/start.ps1/start.sh)与命令行预登录工具(tools/chaoxing_login.py)。
